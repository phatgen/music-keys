# Copy this file to .env.local and set the URL where your server is running.
# The frontend reads this at build time via Vite.
#
# Local development (default — no file needed):
# VITE_API_URL=http://localhost:3001
#
# Deployed server example:
# VITE_API_URL=https://your-server.fly.dev
